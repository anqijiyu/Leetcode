class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        #strip去掉前后的空格
        tmp=s.strip(" ").split(" ")
        return len(tmp[len(tmp)-1])
